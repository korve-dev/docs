{
  "name": "@korve-dev/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run tests/docs.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "validate": "bunx --bun mintlify validate"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
